/**
 * The greening brief moved into components/InquiryForms.tsx when it started
 * submitting for real (2026-09-01, alongside the wedding form; one shared
 * honesty contract, one API). This file survives as a re-export so the
 * greening page's import keeps working.
 */
export { GreeningInquiry as default } from "@/components/InquiryForms";
